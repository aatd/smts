version: '3.8'

services:

  # mongodb:
  #   image: mongo:4.0.8
  #   container_name: mongodb
  #   restart: unless-stopped
  #   command: mongod --auth
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: smts_admin
  #     MONGO_INITDB_ROOT_PASSWORD: mySecretPassword
  #     MONGO_INITDB_DATABASE: smts
  #     MONGODB_DATA_DIR: /data/db
  #     MONDODB_LOG_DIR: /dev/null
  #   volumes:
  #     - mongodbdata:/data/db
  #   networks:
  #     - backend

  frontend:
    
    image: mythief-frontend
    build: 
      context: ./Frontend
    ports:
      - "80:80"
    networks: 
      - frontend
      - backend
  
  backend:
    image: mythief-backend
    build:
      context: ./Backend
    ports: 
      - "5000:5000"
    networks: 
      - backend

networks: 
  frontend:
  backend: